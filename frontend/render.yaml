version: 1
build:
  command: npm install && npm run build
  publish: dist
# SPA routing - final attempt
routes:
  - type: file
    path: /index.html
    execution: server
  - type: rewrite
    source: /*
    destination: /index.html
